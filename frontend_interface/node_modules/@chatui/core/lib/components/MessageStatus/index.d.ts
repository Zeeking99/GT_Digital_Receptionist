/// <reference types="react" />
export declare type IMessageStatus = 'pending' | 'sent' | 'fail';
export interface MessageStatusProps {
    status: IMessageStatus;
    delay?: number;
    maxDelay?: number;
    onRetry?: () => void;
}
export declare const MessageStatus: ({ status, delay, maxDelay, onRetry, }: MessageStatusProps) => JSX.Element | null;
